<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="1200" Height="800">
  <UserControl.Resources>
    <Style TargetType="TextBlock">
      <Setter Property="Foreground" Value="#1A1A1A"/>
      <Setter Property="FontSize" Value="13"/>
    </Style>
    <Style TargetType="Button">
      <Setter Property="Background" Value="#0A6EBE"/>
      <Setter Property="Foreground" Value="#FFFFFF"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="Margin" Value="4"/>
      <Setter Property="Padding" Value="10,6"/>
      <Setter Property="BorderThickness" Value="0"/>
    </Style>
  </UserControl.Resources>
  <Grid Background="#F2F2F2" Margin="12">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <TextBlock Grid.Row="0" FontSize="22" FontWeight="Bold" Foreground="#0A6EBE" Text="Logs"/>
    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
      <TextBlock Text="Level:" VerticalAlignment="Center" Margin="2"/>
      <ComboBox x:Name="CboLevel" Width="100" VerticalAlignment="Center"/>
      <TextBlock Text="Stage:" VerticalAlignment="Center" Margin="8,2,2,2"/>
      <ComboBox x:Name="CboStage" Width="140" VerticalAlignment="Center"/>
      <Button x:Name="BtnRefreshLog" Content="Refresh"/>
    </StackPanel>
    <TextBox Grid.Row="2" x:Name="TxtLog" IsReadOnly="True" VerticalScrollBarVisibility="Auto"
             FontFamily="Consolas" FontSize="12" Background="#FFFFFF" Foreground="#1A1A1A" Margin="0,6,0,0"/>
  </Grid>
</UserControl>
